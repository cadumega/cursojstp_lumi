// Dentro do objeto global window, temos várias propriedades, como alert, só que como o alert recebeu uma função ela é um método e não mais uma propriedade. innerHeight e innerWeight. location tem várias propriedades como o href. Principais funções do js, é alterações pelo DOM.

window.location.href = 'http://google.com';

console.log(window.innerHeight);

console.log(window.innerWidth);

window.alert('Qualquer coisa.');  

== 

alert('Qualquer texto.');